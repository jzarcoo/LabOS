# Practice 1: Bootstrapping and Bare Metal Programming on RP2040

## Build Instructions

Make sure the Pico SDK is installed and the environment variable `PICO_SDK_PATH` is set.

Then run:

``` shell
cd build
cmake ..
make
```
